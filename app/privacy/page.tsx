import { PageShell } from "../components/auth-ui";

export default function PrivacyPage() {
  return <PageShell narrow><section className="auth-card legal"><h1>Privacy Policy</h1><p>Draft placeholder. Final policy must follow Thailand PDPA before launch. It will list data collected (name, email, orders, login provider), purpose, retention, processors (hosting, email, payments), and how to request access or deletion.</p></section></PageShell>;
}
